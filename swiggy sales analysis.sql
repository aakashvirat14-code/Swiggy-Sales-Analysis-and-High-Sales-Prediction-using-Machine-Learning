create database swiggy;
use swiggy;
#data directly load as csv file

#basic questions
#1 display all record
select * from swiggy;
#2 display only city  and order values
select city,order_value from swiggy ;
# 3 show first 10 records
select * from swiggy limit 10;
#4 Count the total number of orders.
select count(order_id) from swiggy;
# 5 find all unique cities
select distinct(city) from swiggy;
#6 display order value greate than 500
select food_category,order_value  from swiggy  where order_value > 500 order by order_value desc;
#7 display order where restaurant_rating greater than 4
select * from swiggy where restaurant_rating > 4 ;
#8 sort all values by descending
select * from swiggy order by order_value desc;
#9 higest order value
select * from swiggy order by order_value desc limit 1;
#10 lowest order value
select * from swiggy order by order_value asc limit 1;

#intermediate level
#11 find average order_value
select avg(order_value) as avg_ordervalue from swiggy;
#12find avgg restraunt rating'
select avg(restaurant_rating)  as avg_rating from swiggy;
#13. Find the total number of orders in each city.
select count(order_id),city from swiggy group by city;
#14 Find the average order value for each city.
select avg(order_value),city from swiggy group by city;
select * from swiggy;
#15 Find the maximum order value in each food category.
select max(order_value),food_category from swiggy group by food_category;
# 16Find the minimum delivery time in each city
SELECT city, AVG(order_value) AS avg_order_value FROM swiggy GROUP BY city;
#17 count cities having more than 20 orders.
SELECT city, COUNT(order_id) AS total_orders FROM swiggy GROUP BY city HAVING COUNT(order_id) > 20;
#18 Find food categories whose average restaurant rating is greater than 4
select food_category, avg(restaurant_rating) from swiggy group by food_category having avg(restaurant_rating ) > 4;
#19 Find cities where the average order value is greater than 400.
select city , avg(order_value) from swiggy group by city having avg(order_value)> 400;
#20 Find food categories having more than 50 orders
SELECT food_category,
       COUNT(order_id) AS total_orders FROM swiggy GROUP BY food_category 
       HAVING COUNT(order_id) > 50;
       
#21 find 2nd higest order values
select order_value from swiggy order by order_value desc limit 1 offset 1 ;
#22 top 5 order values
select order_value from swiggy order by order_value desc limit 5 ;
# 23 find orders greater than avg order  value
select order_value from swiggy where order_value  > ( select avg(order_value) from swiggy)order by order_value desc  ;
#24 categorise order using case
select order_value,city,food_category,
case
    when order_value > 1000 then "HIGH"
    when order_value >500 then "MEDIUM"
    else "LOW"
    end as order_value_classification 
    from swiggy;
#25 rank all orders
select * from swiggy;
#26 find the city with higest avg values
SELECT AVG(order_value),city AS avg_order_value FROM swiggy GROUP BY city ORDER BY avg_order_value DESC LIMIT 1;
#27 find most order food category
select food_category,count(*) as most_ordered from swiggy group by food_category order by most_ordered desc limit 1;
#28 Find customers who placed more than one order
 select customer_id, count(*) as  gt_one from swiggy group by customer_id having count(*) > 1;
#29 Find the customer who spent the most
select customer_id ,sum(order_value) as total_spend from swiggy group by customer_id order by total_spend desc limit 1;
#30 Find total revenue generated by each city
select city,sum(order_value) as total_revenue from swiggy group by city order by total_revenue desc;
#31 Find the average order value for each food category
select food_category,avg(order_value) as avg_value from swiggy group by food_category order by avg_value desc;
#32 Find the top 3 customers by total spending
select customer_id ,sum(order_value) as total_spend from swiggy group by customer_id order by total_spend desc limit 3;
#33 Find the number of orders placed in each city
select city ,count(*) as order_placed_in_city from swiggy group by city order by order_placed_in_city  desc;
# 34 Find food categories with average order value greater than 500
select food_category,avg(order_value) as gt_five_hundred  from swiggy group by food_category   having avg(order_value) > 500;
# 35 find higest order value in each city
select city,max(order_value) from swiggy group by city;
# 36 find min order values
select city,min(order_value) from swiggy group by city;
# 37 find customer whose total spending is above avg cust spending
SELECT customer_id,
       SUM(order_value) AS total_spent
FROM swiggy
GROUP BY customer_id
HAVING SUM(order_value) >
(
    SELECT AVG(total_spent)
    FROM
    (
        SELECT SUM(order_value) AS total_spent
        FROM swiggy
        GROUP BY customer_id
    ) t
);
#39 second most order food  category
select food_category,count(*) as most_order from swiggy group by food_category order by most_order desc limit 1 offset 1; 
#40 show each order with avg oorder value
SELECT *,
       AVG(order_value) OVER (PARTITION BY city) AS city_avg_order
FROM swiggy;